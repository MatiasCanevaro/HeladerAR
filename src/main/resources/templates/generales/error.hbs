{{#partial "contenido"}}

    <div class="d-flex position-absolute top-50 start-50 translate-middle">
        <div class="card-lg">
            <div class="text-center my-2">
                <h1 class="mb-4">{{mensaje}}</h1>
            </div>
            <div class="container text-center">
                <a href={{ruta}} type="button" class="btn btn-primary btn-lg mx-2 mt-4">Ir a {{nombreRuta}}</a>
            </div>
        </div>
    </div>

{{/partial}}
{{> templates/bases/base }}